# vite-v5 (proxy example)

This folder is a Phase-5 compatibility alias for the active Vite React example in:

- `examples/vite-react`

Use this folder as a stable reference path for Phase-5 checklist automation.

## Run

```bash
npm run dev --prefix ../vite-react
```

